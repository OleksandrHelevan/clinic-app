spring:
  application:
    name: chat-service
  data:
    redis:
      host: redis
      port: 6379

chat:
  bucket:
    size: 50
  pagination:
    buckets-per-page: 2

eureka:
  client:
    service-url:
      defaultZone: http://discovery-service:8761/eureka/